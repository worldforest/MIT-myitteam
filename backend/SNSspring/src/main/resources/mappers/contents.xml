<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mit.repo.ContentsRepo">
	<insert id="insert" parameterType="contents">
		<!-- category가 0이면 공모전 1이면 프로젝트 -->
		insert into contents(email, nickname, title, category, description,
		start, end, reward, host, imagesrc, url, views)
		values(#{email}, (select nickname from user where email=#{email}), #{title}, 0, #{description}, #{start}, #{end}, #{reward}, #{host}, #{imagesrc}, #{url}, #{views})
	</insert>
	
	<select id="select" parameterType="contents" resultType="contents">
		select 
	</select>
</mapper>
