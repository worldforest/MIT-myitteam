<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mit.repo.FollowRepo">
	<select id="followingCnt" parameterType="string" resultType="int">
		select count(*) from
		follow where (select nickname from user where email = #{follower}) =
		nickname
	</select>

	<select id="followerCnt" parameterType="string" resultType="int">
		select count(*) from
		follow where (select nickname from user where email = #{following}) =
		following
	</select>

</mapper>
